1. Crear un proyecto maven en inteliJ, añadir fichero pom.xml y sustituir la carpeta src por la que viene creada por defecto.
2. Usar React + Vite y sustituir carpeta src por la que viene creada por defecto.
